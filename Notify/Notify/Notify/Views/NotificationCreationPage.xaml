<?xml version="1.0" encoding="utf-8" ?>

<ContentPage
    BindingContext="{Binding NotificationCreationPage, Source={StaticResource ViewModelLocator}}"
    Title="Notification Creation"
    x:Class="Notify.Views.NotificationCreationPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <StackLayout>


        <Label BackgroundColor="Black" Text="Notification Name" />
        <Entry Text="{Binding NotificationName}" />

        <Label Text="Notification Type" />
        <Picker
            BackgroundColor="Black"
            ItemDisplayBinding="{Binding .}"
            ItemsSource="{Binding NotificationTypes}"
            SelectedItem="{Binding SelectedNotificationType}" />

        <StackLayout BackgroundColor="Black" IsVisible="{Binding IsLocationTypeSelected}">
            <Label Text="Location Option" />
            <Picker ItemsSource="{Binding LocationOptions}" SelectedItem="{Binding SelectedLocationOption}" />
        </StackLayout>

        <StackLayout IsVisible="{Binding IsTimeTypeSelected}">
            <Label Text="Time Option" />
        </StackLayout>

        <Label Text="Friend" />
        <CollectionView ItemsSource="{Binding Friends}" SelectionMode="Multiple">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding Name}" />
                            <CheckBox IsChecked="{Binding IsSelected}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Command="{Binding CreateNotificationCommand}" Text="Create" />

    </StackLayout>

</ContentPage>