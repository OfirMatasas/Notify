<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Notify.ViewModels;assembly=Notify"
             x:Class="Notify.Views.NotificationCreationPage"
             Title="Notification Creation">
    
    <ContentPage.BindingContext>
        <viewModels:NotificationCreationViewModel />
    </ContentPage.BindingContext>
    
    <StackLayout>
        
        <Label Text="Notification Name" />
        <Entry Text="{Binding NotificationName}" />
        
        <Label Text="Notification Type" />
        <Picker ItemsSource="{Binding NotificationTypes}" 
                SelectedItem="{Binding SelectedNotificationType}"
                ItemDisplayBinding="{Binding .}" />
        
        <StackLayout IsVisible="{Binding IsLocationTypeSelected}">
            <Label Text="Location Option" />
            <Picker ItemsSource="{Binding LocationOptions}" 
                    SelectedItem="{Binding SelectedLocationOption}" />
        </StackLayout>
        
        <StackLayout IsVisible="{Binding IsTimeTypeSelected}">
            <Label Text="Time Option" />
            <TimePicker Format="hh:mm tt" 
                         />
        </StackLayout>
        
        <Label Text="Friend" />
        <CollectionView ItemsSource="{Binding Friends}"
                        SelectionMode="Multiple">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Label Text="{Binding .}" />
                            <CheckBox IsChecked="{Binding IsSelected}" />
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
        
        <Button Text="Create" Command="{Binding CreateNotificationCommand}" />
        
    </StackLayout>
    
</ContentPage>