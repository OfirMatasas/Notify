<?xml version="1.0" encoding="utf-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:materialDesignControls1="clr-namespace:Plugin.MaterialDesignControls;assembly=Plugin.MaterialDesignControls"
    x:Class="Notify.Views.LocationSettingsPage"
    BindingContext="{Binding AccountSettingsPage, Source={StaticResource ViewModelLocator}}"
    NavigationPage.HasNavigationBar="False">
    <ContentPage.Content>
        <Grid
            RowDefinitions="Auto, Auto, Auto, *"
            BackgroundColor="{AppThemeBinding Light={StaticResource LightPageBackgroundColor}, Dark={StaticResource DarkPageBackgroundColor}}">
            <StackLayout
                Grid.Row="0"
                Margin="40">
                <Label
                    Text="Location Settings"
                    Style="{StaticResource Body1FontSize_ExoBold}"
                    FontSize="Large"
                    HorizontalTextAlignment="Center">
                </Label>
            </StackLayout>
            <StackLayout 
                Margin="0,30,350,0">
                <ImageButton
                    Source="backButton.png"
                    BackgroundColor="Transparent"
                    Aspect="AspectFit"
                    Command="{Binding BackCommand}">
                </ImageButton>
            </StackLayout>
            <StackLayout
                Margin="20,200,20,0">
                <materialDesignControls1:MaterialPicker 
                    Placeholder="Select destination"
                    FontSize="16"
                    Type="Filled"
                    SelectedItem="{Binding Destination}">
                    <materialDesignControls1:MaterialPicker.ItemsSource>
                        <x:Array Type="{x:Type x:String}">
                            <x:String>Home</x:String>
                            <x:String>Work</x:String>
                            <x:String>School</x:String>
                        </x:Array>
                    </materialDesignControls1:MaterialPicker.ItemsSource>
                </materialDesignControls1:MaterialPicker>
                <materialDesignControls1:MaterialEntry
                    BackgroundColor="White"
                    BorderColor="Transparent"
                    FontSize="16"
                    MaxLength="16"
                    Placeholder="Longitude"
                    ReturnType="Next"
                    TabIndex="1"
                    Text="{Binding Longitude}"
                    TextColor="Black"
                    Type="Filled" />
                <materialDesignControls1:MaterialEntry
                    BackgroundColor="White"
                    BorderColor="Transparent"
                    FontSize="16"
                    MaxLength="16"
                    Placeholder="Latitude"
                    ReturnType="Next"
                    TabIndex="1"
                    Text="{Binding Latitude}"
                    TextColor="Black"
                    Type="Filled" />
                <materialDesignControls1:MaterialEntry
                    BackgroundColor="White"
                    BorderColor="Transparent"
                    FontSize="16"
                    MaxLength="16"
                    Placeholder="Enter Address"
                    ReturnType="Next"
                    TabIndex="1"
                    Text="{Binding SearchText}"
                    TextColor="Black"
                    Type="Filled" />
                <materialDesignControls1:MaterialPicker 
                    Placeholder="Suggestions"
                    FontSize="16"
                    Type="Filled"
                    SelectedItem="{Binding Destination}">
                    <materialDesignControls1:MaterialPicker.ItemsSource>
                        <Binding Path="DropBoxOptions" />
                    </materialDesignControls1:MaterialPicker.ItemsSource>
                </materialDesignControls1:MaterialPicker>
                <Button
                    BackgroundColor="#393E46"
                    Command="{Binding GetAddressSuggestionsCommand}"
                    CornerRadius="30"
                    Text="GET ADDRESS SUGGESTIONS"
                    TextColor="#EEEEEE">
                </Button>
                <Button
                    BackgroundColor="#393E46"
                    Command="{Binding GetGeographicCoordinatesCommand}"
                    CornerRadius="30"
                    Text="GET GEOGRAPHIC COORDINATES"
                    TextColor="#EEEEEE">
                </Button>
                <Button
                    BackgroundColor="#393E46"
                    Command="{Binding UpdateLocationCommand}"
                    CornerRadius="30"
                    Text="UPDATE LOCATION"
                    TextColor="#EEEEEE">
                </Button>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>
